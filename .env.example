# Environment Variables Template for Vercel
# Copy this file and configure the values in Vercel Dashboard

# Database Configuration
# ⚠️ IMPORTANT: SQLite doesn't work on Vercel! Use PostgreSQL
# Get a free PostgreSQL database from:
# - Vercel Postgres (recommended)
# - Supabase
# - Railway
# - Neon
DATABASE_URL="postgresql://user:password@host:5432/database"

# JWT Secret for Authentication
# Generate a secure random string
JWT_SECRET="your-secure-random-jwt-secret-here"

# Google OAuth (if using Google Sign-In)
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="GOCSPX-your-google-client-secret"

# Email Configuration (Optional - for notifications)
EMAIL_USER="noreply@yourdomain.com"
EMAIL_PASS="your-email-app-password"

# Node Environment
NODE_ENV="production"

# ============================================
# CLIENT-SIDE VARIABLES (Not needed on Vercel)
# ============================================
# These are managed in the browser's localStorage:
# - GEMINI_API_KEY (users enter their own key in the AI Agent settings)

# Supabase
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Gemini
GEMINI_API_KEY=your-gemini-api-key
